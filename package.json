{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run check:release && pnpm -r --if-present run build",
    "build:android:aab": "pnpm run check:release && bash ./scripts/build-android-aab.sh",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "check:uk-english": "pnpm --filter @workspace/chainsaw-training run check:uk-english",
    "check:release": "pnpm run check:uk-english && pnpm run typecheck",
    "check:gateway-browser": "playwright test --config playwright.config.ts"
  },
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
